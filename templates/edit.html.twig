{% extends "base.html.twig"%}
{% form_theme formEdit 'bootstrap_4_layout.html.twig' %}
{% block body%}
<h1 style="text-align:center; margin-top:20px;"> Edit a product</h1>

{{form_start(formEdit)}}

{{form_widget(formEdit)}}

<button type="submit" class="btn btn-primary">Save</button>
{{form_end(formEdit)}}

{% endblock %}